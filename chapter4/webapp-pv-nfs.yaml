apiVersion: v1
kind: Pod
metadata:
  name: nfs-webapp
spec:
  volumes:
  - name: nfs-volume
    persistentVolumeClaim:
      claimName: pvc1
  containers:
  - image: nginx
    name: nginx
    volumeMounts:
    - name: nfs-volume
      mountPath: /webdata
